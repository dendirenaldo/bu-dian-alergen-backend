export enum ContentType {
  Page = 'page',
  Article = 'article',
  Announcement = 'announcement',
}

export enum ContentStatus {
  Draft = 'draft',
  Published = 'published',
  Archived = 'archived',
}
